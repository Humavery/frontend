services:
    frontend-app:
        container_name: humavery-frontend
        restart: unless-stopped
        build:
            context: .
            dockerfile: Dockerfile
        env_file:
            - .env
        ports:
            - "${APP_PORT:-3000}:3000"
