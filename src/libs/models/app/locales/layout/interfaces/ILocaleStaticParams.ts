/**
 * @summary Interface for the locale static params.
 * @description Interface for the locale static params.
 */
interface ILocaleStaticParams {
    /**
     * @summary The locale for the static params.
     * @description The locale for the static params. This is the locale that will be used to render the static params.
     */
    readonly locale: string
}

export type { ILocaleStaticParams }
