/**
 * @summary A base class for all client services.
 * @description This class is used as a base class for all client services.
 */
abstract class BaseClientService {
    /**
     * @summary Protected constructor.
     * @description Protected constructor to prevent instantiation of the class, but allow inheritance.
     */
    protected constructor() {}
}

export { BaseClientService }
